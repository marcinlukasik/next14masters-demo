mutation CartChangeItemQuantity(
	$cartId: ID!
	$productId: ID!
	$quantity: Int!
) {
	cartChangeItemQuantity(
		id: $cartId
		productId: $productId
		quantity: $quantity
	) {
		items {
			quantity
		}
	}
}
